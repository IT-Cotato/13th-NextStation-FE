export default function MainPage() {
  return (
    <main className="flex flex-col h-dvh overflow-hidden bg-gray-10 pt-[calc(var(--safe-top)+12px)]">
      <section className="flex justify-center">{/* 기능 구현 전 */}</section>
    </main>
  );
}
